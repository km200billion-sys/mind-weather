import { createTheme } from "@mantine/core";

export const theme = createTheme({
    primaryColor: "indigo",
    defaultRadius: "xl",
    fontFamily: "var(--font-geist-sans), sans-serif",
    // 20대 여성을 위한 감각적인 부드러운 디자인
});
